version: 18
jobs:
- name: GH sync
  triggers:
  - !ScheduleTrigger
    cronExpression: 0 0 */1 ? * * *
    projects: zodiac
  retryCondition: never
  maxRetries: 3
  retryDelay: 30
  cpuRequirement: 250
  memoryRequirement: 256
  timeout: 3600
