
#run main();

main :: () {

    for i := 0; i < 11; i = i + 1 {
        x := i;

        if (x > 1) {
            x = x * x;
        } else if (x == 0) {
            x = -2;
        } else {
            x = -1;
        }

        print("i: ", i, ", x: ", x);
    }

    return 0;
}

