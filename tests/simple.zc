//test

Vec2 :: struct {
    x: s32;
    y: s32;
}

Rect :: struct {
    pos: Vec2;
    size: Vec2;
}

struct_test :: () {
    p : Vec2;
    r : Rect;

    p.x = 40;
    p.y = 2;

    r.pos.x = 1;
    r.pos.y = 2;
    r.size.x = 3;
    r.size.y = 4;

    print(r.pos.x + r.pos.y + r.size.x + r.size.y);

    r2 : Rect;
    r2.pos = p;

    p.x = 2;
    p.y = 40;

    r2.size = p;

    print(r2.pos.x + r2.pos.y + r2.size.x + r2.size.y);
}

main :: () -> s64 {
    a := 8 + 2;
    z :: 19;
    b := z + x + y + foo;

    fuz : s64;
    fuz = 1;

    print(b);
    print(a + b);
    print(add(40, 2));

    test(1);
    test(2);
    test(3);
    test(4);

    print(true);
    print(false);

    pbool1(true);
    pbool1(false);

    pbool2(true);
    pbool2(false);

    print("Hello, Zodiac!");

    struct_test();

    return a + b - 85;
}

foo : s64 = 33;
bar : s64;

test :: (x: s64) {
    if (x == 1) {
        print(1);
    } else if (x == 2) {
        print(2);
    } else if (x == 3) {
        print(3);
    } else {
        print(4);
    }

    print("After if");
}

pbool1 :: (x: bool) {
    if (x == true) {
        print("true");
    } else {
        print("false");
    }
}

pbool2 :: (x: bool) {
    if (x) {
        print("true");
    } else {
        print("false");
    }
}

x :: 21;
y :: 2;

add :: (a: s64, b: s64) {
    return a + b;
}

