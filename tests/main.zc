
#run main();

factorial_iterative :: (n: s64) {

    result := 1;

    for i := 1; i <= n; i += 1 {

        result *= i;
    }

    return result;
}

factorial_recursive :: (n: s64) {

    if n <= 1 return 1;

    return factorial_recursive(n - 1) * n;
}

// rec :: (n: s64)  {
//     if n <= 0 return;
//
//     println(n);
//     rec(n - 1);
// j

main :: () {

    for i := 0; i < 15; i+=1 {

        fi := factorial_iterative(i);
        fr := factorial_recursive(i);

        print(i, ": ", fi, ", ", fr);

        if fi != fr println(" <--- !!!!!! MISMATCH !!!!!!");
        else println(" - match");
    }


    println(factorial_recursive(15));

    // rec(30);

    return 0;
}


