
#run main();

work :: (i: s64) { println(i); }

main :: () {

    // defer println(24);
    // println(0);

    for (i := 0; i < 3; i+=1) {
        j := 1 + (i * 7);

        // defer println(j + 6);
        // println(j);

        // {
        //     defer println(j + 3);
        //     println(j + 1);

        //     work(j + 2);
        // }

        // defer println(j + 5);
        // println(j + 4);

        return 0; // This doesn't work because we emit the loop condition after it.
    }

    // defer println(23);
    // println(22);

    return 0;
}


